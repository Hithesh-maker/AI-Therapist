services:
  - type: web
    name: offline-ai-therapist
    env: python
    plan: free
    buildCommand: |
      python -m pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --timeout 180 --preload backend.app:app